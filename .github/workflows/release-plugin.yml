name: Release Plugin

on:
  push:
    tags:
      - 'plugin-v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Extract version
        id: version
        run: |
          TAG=${GITHUB_REF#refs/tags/}
          VERSION=${TAG#plugin-v}
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

      - name: Validate plugin structure
        run: |
          set -e
          echo "Validating plugin structure..."

          # Required files
          for f in \
            agent-plugin/.claude-plugin/plugin.json \
            agent-plugin/bin/ghx \
            agent-plugin/bin/ghxd \
            agent-plugin/scripts/install.sh \
            agent-plugin/skills/ghxd/SKILL.md \
            agent-plugin/README.md; do
            [ -f "$f" ] || { echo "Missing: $f"; exit 1; }
          done

          # Validate JSON
          python3 -m json.tool agent-plugin/.claude-plugin/plugin.json > /dev/null

          # Validate scripts parse
          bash -n agent-plugin/bin/ghx
          bash -n agent-plugin/bin/ghxd
          bash -n agent-plugin/scripts/install.sh

          # Ensure bin scripts are executable
          [ -x agent-plugin/bin/ghx ] || { echo "bin/ghx not executable"; exit 1; }
          [ -x agent-plugin/bin/ghxd ] || { echo "bin/ghxd not executable"; exit 1; }
          [ -x agent-plugin/scripts/install.sh ] || { echo "scripts/install.sh not executable"; exit 1; }

          echo "✓ Plugin structure valid"

      - name: Stamp version in plugin.json
        run: |
          VERSION=${{ steps.version.outputs.version }}
          cd agent-plugin
          python3 -c "
          import json, sys
          with open('.claude-plugin/plugin.json', 'r+') as f:
              data = json.load(f)
              data['version'] = '${VERSION}'
              f.seek(0)
              json.dump(data, f, indent=2)
              f.write('\n')
              f.truncate()
          "
          echo "Stamped version ${VERSION} in plugin.json:"
          cat .claude-plugin/plugin.json

      - name: Package plugin
        run: |
          VERSION=${{ steps.version.outputs.version }}
          tar -czf "ghxd-plugin-${VERSION}.tar.gz" -C agent-plugin .
          echo "Packaged plugin:"
          tar -tzf "ghxd-plugin-${VERSION}.tar.gz" | head -20

      - name: Compute checksum
        run: |
          VERSION=${{ steps.version.outputs.version }}
          sha256sum "ghxd-plugin-${VERSION}.tar.gz" | tee checksums.txt

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: "Plugin ${{ steps.version.outputs.tag }}"
          generate_release_notes: true
          files: |
            ghxd-plugin-${{ steps.version.outputs.version }}.tar.gz
            checksums.txt
